# ─────────────────────────────────────
# ChemAssist v2 – runtime dependencies
# Requires **Python 3.10 or 3.11** (RDKit wheels are not yet built for 3.12).
# Pin exact versions for reproducibility.
# ─────────────────────────────────────

# ── Core UI ──────────────────────────
streamlit==1.34.0
streamlit-extras==0.4.0

# ── Chemistry & modelling libs ───────
rdkit==2024.03.1          # official wheels on PyPI (cp310, cp311)
PubChemPy==1.0.4
pytesseract==0.3.12       # OCR for screenshot error parsing
PyMuPDF==1.24.2           # fast PDF text extraction
Pillow==10.3.0            # image handling
Jinja2==3.1.4             # input-file templating

# ── Data science stack ───────────────
numpy==1.28.2
pandas==3.0.1
scikit-learn==1.5.0       # optional, for future ML add-ons

# ── LLM & API clients ────────────────
groq==0.3.0               # GroqCloud SDK
openai==1.30.0            # fallback / compatibility
tiktoken==0.6.0

# ── Utilities ────────────────────────
python-dotenv==1.0.1      # load .env vars locally

# ─────────────────────────────────────
# Development / CI extras live in `dev-requirements.txt`.
# ─────────────────────────────────────
